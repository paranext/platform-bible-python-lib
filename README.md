# platform-bible-python-lib
Python library for interacting with Platform.Bible over its WebSocket API

All development happens in the dev branch. The main branch is unlikely to see
much activity, if any.
